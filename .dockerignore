node_modules
npm-debug.log
.git
*.md

# Sandbox data (persisted via volumes, not baked into image)
src/sandbox/*
src/skills/dynamic/*
openclaw.db
src/memory/semantic_core.json

# Dashboard node_modules (rebuilt in Stage 1)
dashboard/node_modules
